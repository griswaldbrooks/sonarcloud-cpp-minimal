sonar.projectKey=griswaldbrooks_sonarcloud-cpp-minimal
sonar.organization=griswaldbrooks

# Multi-language project (C++, JavaScript)
# Phase 0: Examples (blink_led)
# Phase 0.1 (trivial_math) validated and removed
sonar.sources=projects
sonar.tests=projects/examples/blink_led/test

# Exclude test directories from sources
# Note: main.cpp included to satisfy SonarCloud CFamily sensor (requires at least one .cpp file)
# Header-only libraries (blink_controller.h, console_simulator.h) analyzed via coverage report
sonar.exclusions=**/test/**

# C++ configuration
sonar.cfamily.compile-commands=build/compile_commands.json

# C++ coverage
sonar.coverageReportPaths=coverage.xml

sonar.sourceEncoding=UTF-8
